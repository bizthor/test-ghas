spring:
  application:
    name: springhellorest
#  cloud:
#    config:    
#      failFast: ${SPRING_CLOUD_CONFIG_FAILFAST:false}
#      profile: ${SPRING_CLOUD_CONFIG_PROFILE:dev}
#      label: ${SPRING_CLOUD_CONFIG_LABEL:master}
#      uri: ${SPRING_CLOUD_CONFIG_URI:http://config-service:8080}
#      enabled: true